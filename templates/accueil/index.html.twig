{% extends 'base.html.twig' %}
{% block title %}Accueil - Gentlemates{% endblock %}

{% block body %}
    <!-- SECTION HERO (plein écran) -->
    <section class="hero">
        <div class="overlay"></div>
        <div class="hero-content">
            <h1>GENTLEMATES</h1>
            <p>Kind Regards</p>
        </div>
    </section>

    <!-- SECTION ABOUT -->
    <section class="about">
        <div class="container">
            <h2>About Gentlemates</h2>
            
            <div class="about-content">
                <div class="about-image">
                    <img src="{{ asset('image/logo.jpg') }}" alt="Gentlemates Logo">
                </div>
                
                <div class="about-text">
                    <p>
                        Squeezie, Gotaga and Brawks, leading influencers in their respective fields, 
                        have decided to join forces around a common project: an esport club! 
                        Present since the beginnings of esport, and with the strength of their experience, 
                        they are now determined to transmit it in the best possible way and according to the values they cherish. 
                        Gentle Mates is born, everything starts here.
                    </p>
                </div>
            </div>
        </div>
    </section>
{% endblock %}

{% block stylesheets %}
<style>
/* --- HERO --- */
.hero {
    position: relative;
    width: 100%;
    height: 100vh;
    background: url('{{ asset('image/M8-banner.jpg') }}') no-repeat center top/cover;
    display: flex;
    justify-content: center;
    align-items: center;
    color: white;
    text-align: center;
    overflow: hidden;
}

.hero .overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.5);
}

.hero-content {
    position: relative;
    z-index: 2;
    padding: 0 20px;
}

.hero-content h1 {
    font-size: 5rem;
    text-transform: uppercase;
    font-weight: 800;
    letter-spacing: 8px;
    margin-bottom: 20px;
}

.hero-content p {
    font-size: 1.5rem;
    font-style: italic;
    opacity: 0.9;
    letter-spacing: 2px;
}

/* --- ABOUT --- */
.about {
    background: linear-gradient(180deg, #1a1a1a 0%, #0a0a0a 100%);
    color: white;
    padding: 120px 40px;
    text-align: center;
}

.about h2 {
    font-size: 2.5rem;
    margin-bottom: 80px;
    text-transform: uppercase;
    color: #f8b700;
    letter-spacing: 3px;
}

.about-content {
    display: flex;
    align-items: center;
    gap: 60px;
    max-width: 1200px;
    margin: 0 auto;
}

.about-image {
    flex: 1;
    max-width: 500px;
}

.about-image img {
    width: 100%;
    height: auto;
    display: block;
    border-radius: 10px;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5);
}

.about-text {
    flex: 1;
    text-align: left;
}

.about-text p {
    line-height: 2;
    font-size: 1.15rem;
    color: #ddd;
    letter-spacing: 0.5px;
    margin: 0;
}

/* Responsive */
@media (max-width: 968px) {
    .about-content {
        flex-direction: column;
        gap: 40px;
    }
    
    .about-image {
        max-width: 100%;
    }
    
    .about-text {
        text-align: center;
    }
}

@media (max-width: 768px) {
    .hero-content h1 {
        font-size: 3rem;
        letter-spacing: 4px;
    }
    
    .hero-content p {
        font-size: 1.2rem;
    }
    
    .about {
        padding: 80px 20px;
    }
    
    .about h2 {
        font-size: 2rem;
        margin-bottom: 40px;
    }
    
    .about-text p {
        font-size: 1rem;
        line-height: 1.8;
    }
}
</style>
{% endblock %}